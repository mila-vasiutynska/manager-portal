UPDATE leave_requests
SET status = 'REJECTED',
		updated_by = {{appsmith.store.full_name}},
    updated_at = now()
WHERE request_id = {{this.params.request_id}}
  AND status = 'PENDING'
RETURNING request_id;